# 🔴 عاجل: CLAUDE → REPLIT — الإصلاحات موجودة على main، المطلوب النشر فقط

**من:** Claude / Fable 5 · **إلى:** Replit Agent + المالك · **التاريخ:** 2026-07-20
**تصحيح صادق:** فحصت main (`a38cc12`) — **كل الإصلاحات موجودة عليه بالفعل** (عملها المالك/Replit مباشرة). المشكلة **نشر، ليست فقدان كود**.

## ما على `main` بالفعل (متحقّق بالفحص) ✅
| الإصلاح | الدليل على main |
|---------|------------------|
| هيدر Boom Stay أقصر ~5ملي | `StaysHomeHeader.tsx:103` `paddingTop: topPad + 3` |
| كارت «ابحث على الخريطة» يظهر دائماً | `SearchDiscover.tsx` — بوابة `mapAvailable` اتشالت |
| Stay: عملة+دولة في أيقونة | `BookingStaysApp` — `MarketCountryButton` + المصفوفة `stays-market-matrix` اتشالت |
| **الإيجار في أيقونة** | `BookingStaysApp` — `RentalTermPickerButton` (`600b860`) |
| **دورة حياة الاستيراد (بدأت)** | `600b860` «import lifecycle screen» |

## ⇒ المطلوب الوحيد الآن
1. **انشر `main` (`a38cc12`) + أعِد بناء/Reload تطبيق الموبايل** → الإصلاحات تظهر فوراً للمالك. **السبب في عدم ظهورها = النسخة المنشورة خلف main.**
2. **PRs بتاعتي #46/#47/#48 = متجاوزة/مكرّرة** (أنتم عملتم نفس العمل على main مباشرة) — **اقفلوها** لتجنّب اللبس/التعارض. لا تدمجوها (ستتعارض مع نسخة main الأحدث).

## البند الوحيد الباقي في الموبايل (لم يُوحّد بعد)
**مصفوفات RE + Materials** لسه مفروشة: `SectionSearchApp.tsx:1428` `re-market-matrix` + `:1697` `materials-market-matrix`. **أنا أوحّدها على أيقونة `MarketCountryButton`** (نفس نمط Stay اللي تم) على فرع من `a38cc12` + أدفع PR منفصل متحقّق.

## بقية اللوحة الأم (للتتبّع)
دائرة الخريطة (بناء) · إكمال دورة حياة الاستيراد B2B+B2C · مراجعة كاليندر/إشعارات/أصوات/AI بعد النشر. الكل في `CLAUDE-CURSOR-JOINT-FULL-BATCH-MASTER-AR.md`.

**الأولوية القصوى: انشر main — الإصلاحات جاهزة عليه.** أنا أكمّل RE/Materials حالاً.

— Claude / Fable 5 · صدق: main فيه الإصلاحات · المطلوب نشر · PRs القديمة متجاوزة
