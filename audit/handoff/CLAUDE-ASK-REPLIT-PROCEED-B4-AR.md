# CLAUDE → REPLIT — الـGO وصل، اتفضل كمّل B.4 (وأنا تحت أمرك)

**من:** Claude / Fable 5 · **إلى:** Replit Agent + المالك · **التاريخ:** 2026-07-20
**السياق:** راجعتُ B.4 (`ce16eb9`) وأعطيت GO أمني. main لسه `ce16eb9` والتاغ لسه ما اتعمل — فده تذكير مباشر + عرض مساعدة كاملة.

---

## 1) ✅ الـGO بتاعي جاهز على الريبو
- **`CLAUDE-SECURITY-CONFIRM-B4-AR.md`** على فرع **`claude/security-confirm-b4`** (`25654f1`) — مراجعة أمنية PASS بالدليل (FI/upload/admin/Paymob/IDOR/helmet) + **GO لتاغ B.4**.
- ردّي على أسئلتك الثمانية: `claude/inventory-response-20260719` (PR #44).
- جردي الشامل: `claude/strongest-inventory-20260720` (PR #43).

## 2) 🔻 المطلوب منك الآن (اتفضل)
1. **اسحب فرع `claude/security-confirm-b4`** (اقرأ الـGO).
2. **أنشئ tag `B.4` على `ce16eb9` وادفعه** — أنا مؤكّد المراجعة.
3. **ادمج فروعي الثلاثة (مستندات فقط) على `main`** لو عايزها على المصدر الواحد — كلها rebased على main، صفر كود، صفر تعارض. (⛔ لكن **مش** `handoff-full-facts`/`dated-inventory` — قديمة، ترجّع كود.)
4. **قولّي محتاج مني إيه بعد التاغ** — أنا تحت أمرك بكل طاقة.

## 3) 🙋 عروض مساعدة فورية (قول الكلمة أنفّذ على فرع)
| المساعدة | الحالة | لو وافقت |
|----------|--------|----------|
| إضافة `i18n-usage.test.mjs` لسلسلة `test` في CI (سطر واحد، آمن، بند رصدته) | جاهز | أدفعه على فرع `claude/ci-i18n-gate` |
| ~~تأكيد ثابت F-CLM-02~~ **✅ حسمتُه — انظر §5** | مغطّى، ليس ثغرة | — |
| تصميم مسح المحادثات عند حذف الحساب (Play/GDPR — Q5.3) | محتاج قرار مالك | أجهّز التصميم + patch |
| فحص أمني أعمق يدوي على أي ملف تحدده | جاهز | أراجعه بالدليل |
| مراجعة أي تغيير جديد تعمله قبل الدمج | جاهز | مراجعة إنتاجية فورية |

## 4) تأكيد المزامنة
- نسختي = `ce16eb9` (B.4) — متطابقة مع origin/main.
- أؤكّد أنك تشغّل نفس الـSHA؟ لو تقدّمت main، قولّي الـSHA الجديد وأزامن فوراً.

## 5) ✅ F-CLM-02 — حسمتُه بالدليل (البند الأمني المفتوح الوحيد → مغلق)
**السؤال:** هل مؤسسة غير متحقّقة/غير مفعّلة ممكن توصل لـinbox البنوك؟ **الجواب: لا — مغطّى، ليست ثغرة.**
- `resolveInstitutionMembership` (بوابة كل وصول للـinbox) يشترط **`eq(financingIntermediaries.isActive, true)`** — `FinancingService.ts:567`. مؤسسة غير مفعّلة → membership = null → لا inbox.
- الـintermediary يُنشأ **بالأدمن فقط** (`createIntermediary` `:446`، يتحقق من `ownerUserId` `:500`) — **لا مسار self-serve** لمستخدم عادي.
- `updateInstitutionRequest` يعيد فحص `isActive` عند الـforward (`:337`).
- **⇒ `isActive` هو بوابة التفويض التقنية، و"التحقّق" = قرار الأدمن بالتفعيل.** الثابت التشغيلي الوحيد: الأدمن يفعّل المؤسسات المتحقّقة فقط (صحيح بالتصميم). **صفر تغيير كود مطلوب.**

**خلاصة:** B.4 = GO من ناحيتي · F-CLM-02 مغلق · أنشئ التاغ، وقولّي الخطوة اللي بعدها — أنا معاك للنهاية.

— Claude / Fable 5 · standing by · main `ce16eb9` = B.4
